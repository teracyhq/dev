# teracy-dev system config, not overriable
vagrant:
  require_version: ">= 2.2.0" # require >= 2.2.0 to have full trigger support
  # rubygems (vagrant plugins)
  plugins: []
    # - name: wisper
    #   version: ""
    #   state: installed # or uninstalled
