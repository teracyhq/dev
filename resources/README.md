teracy-dev's resources
======================

Contain all common and useful resources for Teraciers.

project-template
----------------

`project-template` includes common resources needed for creating a new project.

``` bash
$ mkdir <project_name>
$ cd <project_name>
$ git init
$ cp -r /vagrant/resources/project-template/. .
```
